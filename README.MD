# 项目概述

这是一个构建用于 claude code 的提示词的项目。

你需要帮我完成接下来的任务

## 接下来的任务

目前已经有了一些文档。

接下来我希望再为它添加一些提示，把这些提示转为更合适、精准的表达写入到CLAUDE-appoint.md中合适的位置：
- 必须在每一次要用到实时时间时，执行`python -c "import datetime; print(datetime.datetime.today())"`来获取本地时间