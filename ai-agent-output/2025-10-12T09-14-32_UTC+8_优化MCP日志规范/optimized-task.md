# 优化后任务描述

## 任务概述
- 针对general目录的指令体系新增两类规范：
  1. 更新`AI-AGENT_mcp-rules.md`以支持基于CSV的MCP调用记录，并明确“先取时间戳→执行→写入”的流程。
  2. 强化“任务前置准备”各子环节的执行要求，确保进行多维度优化分析、记录待定项，并在获得用户确认前不得进入后续环节。

## 执行步骤（待用户确认后生效）
1. 根据用户选择的U001方案，修订`general/AI-AGENT_mcp-rules.md`中所有涉及`mcp-log`的要求，新增CSV字段定义、操作流程与例外说明；必要时同步调整示例与日志记录流程文档。
2. 依据用户对U002的决策，扩写`general/AI-AGENT_general.md`及关联文档（`AI-AGENT_step-before.md`、`AI-AGENT_check.md`），补充全面分析、待定项列举、无优化时需提供评估依据和必须等待用户确认的强制要求。
3. 按照选定方案实施文档改动后，更新相关检查清单及流程图（若涉及），并记录变更原因。

## 验证标准
- `general/AI-AGENT_mcp-rules.md`中全部`mcp-log`引用均切换为CSV要求，并写明字段、调用顺序、豁免条件；说明结构清晰、示例可复用。
- `general/AI-AGENT_general.md`与对应支撑文档明确列出“多维优化分析→待定项记录→用户确认→方可进入下一环节”的闭环指令，即使判定无优化空间也需说明评估依据。
- 更新后的规范通过`AI-AGENT_check.md`逐项对应，确保检查清单能验证上述要求。
- 相关文档语义一致、无矛盾表述，且新增内容符合现有格式与语言风格。

## 待确认事项
- U001：选择采用哪种MCP日志增强方案（S001或S002）。
- U002：选择任务前置准备执行强化策略（S001或S002）。

> 在用户确认待定项并指明采纳方案前，暂停后续环节的执行。
