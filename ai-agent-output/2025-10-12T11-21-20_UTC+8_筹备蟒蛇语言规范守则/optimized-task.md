# 优化后任务说明

## 背景
- 用户要求在不再追加确认的前提下，完成通用指令库中与Rust、Python规范守则相关的更新工作。
- 已确认采用以下结论：
  1. `general/criterion-rust/`目录内所有`规范指导_*`文件需更名为`规范守则_*`，并同步更新文档内的术语与标题。
  2. Python规范守则按“CLI与自动化脚本”“Web与微服务”“数据科学与机器学习”“企业级平台与服务”“高性能与分布式计算”五大方向编写。
- 任务期间遵循`user-temp-opinion.md`：不创建`mcp-log.csv`。

## 目标
1. 完成Rust规范文档文件名及正文术语的统一调整，确保引用和结构保持一致。
2. 调研Python生态下各方向的最佳实践与权威资料，形成可落地的规范守则集合，并沉淀来源链接。
3. 更新必要的索引、说明或引用文档，保证读者可快速定位新文档。
4. 全流程纳入状态记录、变更文档与提交管理，符合项目规范。

## 执行步骤
1. **Rust文档同步**
   - 列出`general/criterion-rust/`内所有目标文件，规划重命名映射。
   - 执行重命名并批量替换文档中“规范指导”相关措辞为“规范守则”。
   - 使用`rg`检查是否存在遗漏的旧术语，必要时手动修正。
   - 检视引用文档（如`general/README.md`等）并同步更新名称。
2. **Python规范守则筹备**
   - 查阅`rust/`目录下相关指南，归纳可复用的结构或写法。
   - 针对五大方向开展在线资料调研（官方文档、PEP、框架白皮书、社区最佳实践、优秀开源项目），整理要点及链接。
   - 设计统一的文档框架：背景定义、项目特点、架构/代码规范、工具依赖、测试与运维、参考资料、案例链接。
3. **文档撰写与归档**
   - 在`general/criterion-python/`目录下为五个方向分别创建`规范守则_*`文档，按统一结构写作。
   - 若目录不存在则新建，并编写必要的索引或README。
   - 每篇文档确保包含引用来源、实践建议和适用场景说明。
4. **验证与文档维护**
   - 按`AI-AGENT_check.md`执行验证：
     - `rg`确认无残留“规范指导”。
     - 检查Python文档中链接格式与内容完整性。
     - 对照成功标准确保覆盖目标。
   - 更新`CHANGELOG.md`（重建`[Unreleased]`段落与新版本条目），记录主要变动。
   - 更新相关readme或索引文件的导航信息。
5. **提交与收尾**
   - 在每次状态更新后执行`git add`并提交，保持原子性。
   - 记录最终状态，清理临时文件，恢复`now-task.md`模板。

## 成功标准
- Rust规范文档文件名与内部标题全部统一为“规范守则_*”，不存在遗漏的旧术语。
- `general/criterion-python/`目录下存在五篇结构化文档，覆盖所列方向，内容包含：核心原则、项目结构建议、关键工具或库、质量保证措施、参考资料链接（含来源说明）。
- 所有引用（README、索引）均已更新，访问路径正确。
- 状态记录、文档更新与`CHANGELOG.md`维护符合项目流程要求，提交历史清晰可追溯。
- 通过`AI-AGENT_check.md`中对应阶段的核对项，无未解决风险。
