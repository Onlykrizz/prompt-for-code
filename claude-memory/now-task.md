## 现在的任务

全面分析claude-memory下现有的所有文档，然后将下述流程进行重新组织综合优化后加入到现有文档中：

- 每次开始任务之前，要检查claude-memory目录下所有文件名以`CLAUDE`开头的文件：
  - 看这些文件在上一次git提交之后是否有变更
  - 每当完成一个步骤的一批文档的修改后，都要先进行仓库提交再进行下一步，
  - 如果有没被提交的改动，就分析没提交的文件和改动的内容，然后进行提交并在提交信息中描述当前状态，
- 当任务完成后，要维护CHANGELOG并进行提交，在这一次提交之后：
  - 删除CLAUDE.md中**本次任务内容**开始后面的所有内容
  - 删除now-task.md中除了第一行之外的所有内容
  - 删除undetermined.md文件
  - 结束掉CHANGELOG中本次的Unreleased，分析应该更新哪一段版本号，更新相应的版本号，添加新的Unreleased
- 需要注意，不论是什么任务，不论是普通的还是程序代码开发任务，都应该要：
  - 维护CHANGELOG，在每一次提交之前检查仓库变更状态，分析变更内容以决定是否需要对CHANGELOG进行修改

